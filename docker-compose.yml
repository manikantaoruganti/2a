version: "3.9"

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - TZ=UTC
    volumes:
      - seed-data:/data
      - cron-output:/cron
    restart: unless-stopped

volumes:
  seed-data:
  cron-output:
